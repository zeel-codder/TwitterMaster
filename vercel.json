{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "distDir": "server/dist" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server/dist/app.js" }]
}
