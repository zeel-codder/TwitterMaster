{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/server/dist/server.js" }],
  "builds": [{ "src": "/server/dist/server.js", "use": "@now/node" }],
  "engines": {
    "node": "14.x"
  }
}
